import styled from 'styled-components';

export const SubmitButtonWrapper = styled.div`
  display: flex;
  gap: 2rem;
  margin-top: 1rem;
`;

export const CollectionFormHeader = styled.header`
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 1rem;
  text-align: center;
  margin-bottom: 2rem;
`;

export const CollectionFormBody = styled.div`
  display: flex;
  flex-direction: column;
  gap: 2rem;
`;

export const FormWrapper = styled.section`
  margin: 0 10vw;
`;
